# Chord Analyzer — Frontend

This folder contains a minimal Next.js single-screen frontend scaffold that pairs with the Python API in the repo root.

Quick start:

```bash
cd web
npm install
npm run dev
```

Open http://localhost:3000 to view the UI. The demo Analyze button calls `/api/health` (a placeholder); replace it with your actual analyze endpoint.
